# `interfaces`

## Purpose

Reserved for **user and system entrypoints**: chat, admin, CLI, bots, HTTP API, webhooks, IDE-style channels — the **Interface Layer** in `web-gpt-sources/02_architecture.md` and `11_interfaces_runtime.md`.

## Relation to MARS architecture

Implements the **Interface Layer**. Feeds the **Control Plane** and must respect **Security** and **Observability** concerns when implemented.

## Status

**Planned** — placeholder only.
