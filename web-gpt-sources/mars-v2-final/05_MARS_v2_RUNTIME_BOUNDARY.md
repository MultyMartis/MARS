# MARS v2 — Runtime boundary

**Status:** **BOUNDARY ONLY** · **EXPERIMENTAL** (narrow JS)

---

## Hard line

| MARS is | MARS is not |
|---------|-------------|
| v0 **contracts** under `mars-runtime/*.md` | Production multi-agent runtime |
| Optional **R1** manual `node` demos | Autonomous orchestration |
| Honesty boundary for future runtime | Hidden coordination or deploy narrative |

**Do not upload `mars-runtime/**/*.js` to Web-GPT** as proof — boundaries only in this pack.

---

## R1 (if repo cited)

| Fact | |
|------|--|
| Entry | Human-invoked scripts only |
| Scope | Narrow handoff (e.g. n8n webhook) |
| Registry JS | Demo keys — not `tools/registry.md` SoT |
| Missing | Queue, orchestrator, concurrency, long-running host |

**File exists** ≠ **fleet operational**.

---

## Contracts vs code

| Artefact | Status |
|----------|--------|
| `execution-bridge-v0.md`, queue/orchestrator contracts | **CORE** documentation |
| `adapters/`, `runtime/*.js` | **EXPERIMENTAL** — boundary context |

Execution Bridge = translation concept — **no** claim of deployed bridge **instance**.

Control plane / workflow engine / autonomous dispatch = **planned**, not shipped MARS.

---

## When to touch `mars-runtime/`

Only with **Runtime** lane + explicit charter:

- Run documented scripts in controlled env  
- REPORT with paths — no production claims  
- Keep separate from Factory delivery and governance rewrites  

Otherwise: **read-only** boundary context for planning.

---

## SAFE UNKNOWN

- Production deployment topology for future runtime  
- Team adoption of n8n as standard bridge  
- Live parity: adapter code ↔ external workflow graphs  

*Registry split detail: `01` — governance markdown vs R1 JS vs external catalogs.*
