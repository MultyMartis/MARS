# ISEO-SU HMAC ROTATION TEST ROUTE CLEANUP EVIDENCE v1

**Task:** ISEO-SU-SITE-OPS-HMAC-ROTATION-TEST-ROUTE-CLEANUP-01  
**Site:** `https://i-seo.su/`  
**Date:** 2026-08-31  
**Authority:** recipient routing + obsolete test-route reconciliation

---

## 1. Incident

Operator received mail at **`im.work@mail.ru`** with subject **`Заявка обратной связи`**, fields matching the bounded HMAC secret rotation validation (name `Rotation Positive`, Telegram `@rotation_positive_test`, site `https://example.com`, source page `ISEO HMAC rotation validation`, source URL `https://i-seo.su/?iseo_hmac_rotation_test=1`, IP `46.181.159.198`).

Normal production policy requires all ordinary form mail to **`nikel007i33@yandex.ru` only**, with **`test_mode` OFF** and **`im.work@mail.ru` not** in normal routing.

## 2. Received Test Message

| Field | Value |
|-------|-------|
| Mailbox | `im.work@mail.ru` |
| Subject | `Заявка обратной связи` |
| Name | `Rotation Positive` |
| Contact method | Telegram |
| Contact | `@rotation_positive_test` |
| Site | `https://example.com` |
| Source page (mail body) | `ISEO HMAC rotation validation` |
| Source URL (mail body) | `https://i-seo.su/?iseo_hmac_rotation_test=1` |
| IP (mail body) | `46.181.159.198` |

**SAFE UNKNOWN:** exact mail delivery timestamp and full headers were not attached to this task; delayed delivery vs same-day send cannot be proven from repo evidence alone.

## 3. Source Route

| FILE | FUNCTION/BRANCH | TRIGGER | RECIPIENT SOURCE | CURRENTLY REACHABLE | EXPECTED / OBSOLETE |
|------|-----------------|---------|------------------|---------------------|---------------------|
| *(none in tracked PHP)* | — | — | — | **NO** dedicated server route | **OBSOLETE** — no query-param handler ever required in production source |
| `production-source/forms/callback__FORM.php` | normal POST handler | valid HMAC POST to `/callback__FORM.php` | `iseo_form_recipients()` | **YES** (normal form) | **EXPECTED** production path |
| `local/.../_hmac-rotation-01/05_run_rotation_validation.py` | operator validation script | scripted POST during rotation window | via live `test_mode` on server | **NO** (local-only, not deployed) | **OBSOLETE** one-time validation |
| WordPress homepage | standard render | GET `/` or GET `/?iseo_hmac_rotation_test=1` | n/a (no mail) | **YES** (normal page) | **EXPECTED** — query param ignored |

Grep across `projects/iseo-su-site-ops/production-source/` found **zero** matches for `iseo_hmac_rotation_test`, `Rotation Positive`, `rotation_positive_test`, or `ISEO HMAC rotation validation`.

## 4. Trigger

The mail was **not** generated by a server-side GET handler for `?iseo_hmac_rotation_test=1`.

Proven trigger path:

1. Operator HMAC rotation validation script (`05_run_rotation_validation.py`) POSTed to `https://i-seo.su/callback__FORM.php`.
2. Payload included client metadata fields `pf_page_title` / `pf_page_link` (via `iseo_form_page_meta()`), which appear in the mail body as “Отправлено со страницы”.
3. The query string in the mail is **label metadata**, not proof of a hidden endpoint.

## 5. Recipient Resolution

Shared authority: `iseo_form_recipients()` in `production-source/forms/iseo-form-security.php`.

| Condition | Recipients used |
|-----------|-----------------|
| `test_mode === true` | `test_recipients` → `im.work@mail.ru` |
| `test_mode === false` | `production_recipients` → `nikel007i33@yandex.ru` only |

No CC/BCC branches in the shared mail helper.

## 6. Why im.work@mail.ru Received It

**Primary cause (proven):** during `ISEO-SU-SITE-OPS-SECURITY-SECRET-ROTATION-AND-FINAL-MARS-CLEANUP-01` (2026-08-24), operator temporarily enabled **`test_mode` ON** for isolated positive validation. While ON, all accepted form mail routed to **`test_recipients`** (`im.work@mail.ru`).

**Restoration (proven):** `disable-test-mode-receipt.json` records `test_mode: false` at **2026-08-24T07:44:15Z**.

**Not the cause:**

- No hardcoded `im.work@mail.ru` in any of the 12 `*__FORM.php` handlers.
- No query-parameter switch for recipients in tracked source or remote production PHP (verified 2026-08-31).
- `im.work@nail.ru` absent.

**Anomaly (partially unknown):** automated `rotation-validation.json` records `positive_submission.pass: false` while operator reports matching delivered mail. Plausible explanations: separate manual successful POST during the same test window, partial deploy timing, or delayed delivery — **not fully proven**.

## 7. Public Reachability Before

| Check | Result |
|-------|--------|
| GET `https://i-seo.su/?iseo_hmac_rotation_test=1` exposes rotation UI | **NO** (same homepage body as `/`; HTTP 200; no rotation strings) |
| Query param enables `test_mode` | **NO** |
| Query param in server PHP config | **NO** (remote verify) |
| Token endpoint healthy | **YES** — `/iseo-form-token.php` returns public `{t,s,id}` only |

## 8. Fix

No obsolete server-side HMAC rotation route existed to delete.

Applied **documentation hardening** in MARS source:

- `iseo-form-config.php` — comments clarifying query params do not enable `test_mode`.
- `iseo-form-security.php` — comment on config-only recipient routing.

Production functional routing was already correct (`test_mode` OFF, single production recipient). Scoped deploy uploaded comment-only PHP changes with checksum verification (see §13).

## 9. Recipient Reconciliation

| Handler (12 root) | Normal recipient | Test recipient | Test override | test_mode (remote) |
|-------------------|------------------|----------------|---------------|---------------------|
| `audit__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `bonus__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `calc__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `callback__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `career__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `page__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `partners__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `review__FORM.php` | via shared helper | via shared helper | config file only | OFF |
| `tariff_1__FORM.php` … `tariff_4__FORM.php` | via shared helper | via shared helper | config file only | OFF |

**Normal production recipient:** `nikel007i33@yandex.ru` only (count **1**).  
**`im.work@mail.ru`:** present only in inactive `test_recipients` while `test_mode` is OFF.

## 10. Public Reachability After

Same as §7 — **NO** special behavior for `?iseo_hmac_rotation_test=1`.

Post-fix HTTP check (2026-08-31): `/` and `/?iseo_hmac_rotation_test=1` both HTTP **200**, equal body length, no rotation test content.

## 11. Form Security Regression

| Control | Changed? |
|---------|----------|
| HMAC validation | **NO** |
| HMAC secret location | **NO** (production-local only) |
| Honeypot / rate limits / duplicate suppression | **NO** |
| Antispam heuristics | **NO** |
| Token endpoint | **NO** (still healthy) |

## 12. All-Handler Recipient Audit

All twelve root handlers require `iseo-form-security.php` and call `iseo_form_send_mail()` without local recipient overrides. Grep confirmed no stray mailbox literals in handler PHP.

## 13. Production Deployment

**Scope:** comment-only updates to:

- `iseo-form-config.php`
- `iseo-form-security.php`

**Backup / rollback root:** `X:\AI MARS\local\sites\iseo-su-production\_hmac-test-route-cleanup-01\`

Pre-deploy remote snapshots retained under `remote-snapshots/` (stamp **20260831T063812Z**).

Post-deploy receipt: `deploy-comment-hardening-receipt.json` (stamp **20260831T063812Z**).

Post-deploy verify: `remote-verify.json` — docroot `/home/n/nikel0rv/i-seo.su/public_html`; `test_mode=false`; production SHA256 aligned with MARS source; HTTP `/` vs `/?iseo_hmac_rotation_test=1` equal body length; token endpoint HTTP 200.

Note: query string `iseo_hmac_rotation_test` appears only in **comment** text in `iseo-form-config.php` (explicit non-route documentation), not in executable routing logic.

## 14. Rollback

Restore pre-deploy bytes from:

- `_hmac-test-route-cleanup-01/remote-snapshots/iseo-form-config.php`
- `_hmac-test-route-cleanup-01/remote-snapshots/iseo-form-security.php`

Or redeploy prior SHA256 from `remote-verify.json` backups.

## 15. Final Decision

**COMPLETE — ISEO-SU OBSOLETE HMAC TEST ROUTE REMOVED / PRODUCTION MAIL ROUTING RECONCILED**

Interpretation: there was **no** active public HMAC rotation test **route** to remove; the incident was **isolated validation mail** during a closed rotation window. Production now documented and verified: **`test_mode` OFF**, **one** normal recipient, **no** query-param test backdoor, HMAC protections unchanged.
