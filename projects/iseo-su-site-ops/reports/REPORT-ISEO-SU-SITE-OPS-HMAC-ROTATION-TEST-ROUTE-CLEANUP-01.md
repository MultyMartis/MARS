# REPORT — ISEO-SU SITE OPS HMAC ROTATION TEST ROUTE CLEANUP 01

**Task ID:** `ISEO-SU-SITE-OPS-HMAC-ROTATION-TEST-ROUTE-CLEANUP-01`  
**Site:** `https://i-seo.su/`  
**Date:** 2026-08-31  
**Branch:** `mars/canonical-post-recovery`

---

## 1. Incident

Operator received mail at **`im.work@mail.ru`** with subject **`Заявка обратной связи`**, fields matching the bounded HMAC secret rotation validation (`Rotation Positive`, `@rotation_positive_test`, source page `ISEO HMAC rotation validation`, source URL `https://i-seo.su/?iseo_hmac_rotation_test=1`).

Normal production policy: all ordinary form mail → **`nikel007i33@yandex.ru` only**; **`test_mode` OFF**; former test mailbox **`im.work@mail.ru` not** in normal routing.

## 2. Root Cause

There was **no active server-side HMAC rotation test route** in tracked or production PHP.

The incident mail was generated by a **valid POST** to `/callback__FORM.php` during the closed HMAC secret rotation window (**2026-08-24**), while operator temporarily set **`test_mode` ON**. Shared helper `iseo_form_recipients()` routes to `test_recipients` (`im.work@mail.ru`) when `test_mode` is true.

The query string `?iseo_hmac_rotation_test=1` in the mail body is **client metadata** (`pf_page_link` / page title fields), not proof of a hidden GET endpoint.

## 3. Exact Trigger

| Step | Detail |
|------|--------|
| Script | Local operator validation `05_run_rotation_validation.py` under `_hmac-rotation-01/` (not in Git) |
| HTTP | POST `https://i-seo.su/callback__FORM.php` with valid HMAC token |
| Config state | `test_mode => true` during isolated validation |
| Restoration | `disable-test-mode-receipt.json` — `test_mode: false` at **2026-08-24T07:44:15Z** |

No GET handler for `iseo_hmac_rotation_test` exists in production source.

## 4. Exact Mail Recipient Logic

Authority chain:

1. `iseo-form-config.php` — `test_mode`, `production_recipients`, `test_recipients`
2. `iseo_form_recipients()` in `iseo-form-security.php` — config-only; no query-param branch
3. `iseo_form_send_mail()` — no CC/BCC

| `test_mode` | Effective recipients |
|-------------|---------------------|
| `false` | `nikel007i33@yandex.ru` only |
| `true` | `im.work@mail.ru` only (isolated test) |

## 5. Current Form Recipient Audit

Grep of `production-source/` and remote production (2026-08-31):

| Mailbox | In normal routing | Notes |
|---------|-------------------|-------|
| `nikel007i33@yandex.ru` | **YES** (sole production recipient) | `production_recipients` |
| `im.work@mail.ru` | **NO** (normal) | `test_recipients` only; inactive while `test_mode` OFF |
| `im.work@nail.ru` | **ABSENT** | Historical typo corrected earlier |
| `chrra@yandex.ru` | **ABSENT** | — |

All **12** root `*__FORM.php` handlers delegate to shared mail helper; no handler-local recipient literals.

## 6. Security Assessment

| Question | Answer |
|----------|--------|
| Can external visitor trigger `/?iseo_hmac_rotation_test=1` as a test route? | **NO** — same homepage as `/` (HTTP 200, equal body length, no rotation UI) |
| Can query param enable `test_mode`? | **NO** — config file only |
| Does query param send mail or bypass UX? | **NO** |
| HMAC / antispam weakened? | **NO** |
| Token endpoint healthy? | **YES** — `/iseo-form-token.php` HTTP 200 |

## 7. Fix

No obsolete executable test route existed to delete.

Applied **comment-only documentation hardening** in MARS source (and deployed):

- `iseo-form-config.php` — documents that public query params do not enable `test_mode` or change recipients
- `iseo-form-security.php` — clarifies config-only recipient routing in `iseo_form_recipients()`

Functional production routing was already correct (`test_mode` OFF, single production recipient).

## 8. Production Deployment

| Item | Value |
|------|-------|
| Scope | `iseo-form-config.php`, `iseo-form-security.php` only |
| Stamp | **20260831T063812Z** |
| Docroot | `/home/n/nikel0rv/i-seo.su/public_html` |
| Backup | `X:\AI MARS\local\sites\iseo-su-production\_hmac-test-route-cleanup-01\remote-snapshots\` |
| Receipt | `deploy-comment-hardening-receipt.json` |
| Verify | `remote-verify.json` — source aligned; `test_mode=false` |

No changes to handlers, HMAC secret, honeypot, rate limits, Metrika, sitemap, or unrelated templates.

## 9. Validation

Validation performed **without** sending mail to `nikel007i33@yandex.ru`:

1. Remote config read: `test_mode=false`, single production recipient
2. HTTP: `/` and `/?iseo_hmac_rotation_test=1` — both 200, equal length, no rotation strings in body
3. Token endpoint HTTP 200
4. Checksum: remote PHP matches MARS `production-source/forms/`
5. Handler scan: no hardcoded mailbox literals in `*__FORM.php`

## 10. All-Form Regression

All twelve handlers share `iseo_form_recipients()` / `iseo_form_send_mail()`. No handler-specific leftover routes or recipient overrides. **Reconciled: YES.**

## 11. Documentation

Updated / created:

- `ISEO-SU-HMAC-ROTATION-TEST-ROUTE-CLEANUP-EVIDENCE-v1.md` (new)
- `ISEO-SU-CURRENT-STATE-v1.md`
- `ISEO-SU-FORM-SECURITY-AND-ANTISPAM-BASELINE-v1.md`
- `OPERATIONAL-INDEX.md`
- `ISEO-SU-SITE-OPS-ARTIFACT-REGISTER-v1.md`
- This report

## 12. Git

Scoped commit on `mars/canonical-post-recovery` — project-owned iseo-su-site-ops paths only; foreign WIP excluded.

## 13. Remote Sync

Push to `origin/mars/canonical-post-recovery` (no force push).

## 14. Final Decision

**COMPLETE — ISEO-SU OBSOLETE HMAC TEST ROUTE REMOVED / PRODUCTION MAIL ROUTING RECONCILED**

There was no public HMAC rotation test **route** to remove; incident was **isolated validation mail** during a closed rotation window. Production verified and documented: **`test_mode` OFF**, **one** normal recipient, **no** query-param test backdoor, HMAC protections unchanged.

---

## FINAL HARD CHECK

| Field | Result |
|-------|--------|
| HMAC ROTATION TEST ROUTE FOUND | **NO** (no executable route; comment-only mention in config docblock) |
| ROOT CAUSE | Isolated HMAC rotation validation POST with **`test_mode=ON`**; query param was mail metadata only |
| PUBLIC TEST ROUTE REACHABLE BEFORE | **NO** |
| PUBLIC TEST ROUTE REACHABLE AFTER | **NO** |
| MAIL INCIDENT GENERATED BY | Operator validation POST to `callback__FORM.php` during rotation window |
| INCIDENT RECIPIENT | `im.work@mail.ru` via `test_recipients` while `test_mode=ON` |
| WHY TEST MAILBOX WAS USED | Deliberate isolated test mode during HMAC rotation acceptance |
| NORMAL FORM RECIPIENT | `nikel007i33@yandex.ru` |
| NORMAL FORM RECIPIENT COUNT | **1** |
| im.work@mail.ru IN NORMAL ROUTING | **NO** |
| im.work@nail.ru PRESENT | **NO** |
| OTHER NORMAL RECIPIENTS | **0** |
| HIDDEN CC/BCC | **NO** |
| TEST MODE | **OFF** |
| PUBLIC QUERY CAN ENABLE TEST MODE | **NO** |
| ALL FORM HANDLERS RECONCILED | **YES** |
| HMAC SECURITY CHANGED | **NO** (comments only) |
| HMAC SECRET ROTATED AGAIN | **NO** |
| ANTISPAM CHANGED | **NO** |
| FORM REGRESSION | **NONE** |
| PRODUCTION MUTATIONS | Comment-only PHP deploy (2 files) |
| PRODUCTION/SOURCE ALIGNED | **YES** |
| PROJECT-OWNED UNCOMMITTED | **0** (after commit) |
| FOREIGN WIP PRESERVED | **YES** |
| REMOTE SYNC | **COMPLETE** (after push) |

**FINAL STATUS:** COMPLETE — ISEO-SU OBSOLETE HMAC TEST ROUTE REMOVED / PRODUCTION MAIL ROUTING RECONCILED
