# WORKSTREAM-C-PRESERVATION

Any future manually authorized SAFE_TO_RETRY must still use Workstream C lifecycle (charter, active=false initial, lock, preflight, readiness GET, bounded window, max one request, unconditional deactivation, re-containment).
No retry may bypass Workstream C. D6E2B does not implement Workstream D.
