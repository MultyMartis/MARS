# Canonical Commit

Work model: clean worktree under `X:\AI MARS STORAGE\git-sync-d6g1b-*\repo`.

Base before D6G1B commits: `b8724c98` on `origin/mars/canonical-post-recovery` (rebase after interim remote advance).

Evidence commit:

- `a7bd33a6` — `docs(site-002): verify Beget watchdog cron and accept natural D6G1 runs`

Tip / gate record commit:

- `32e1502c` — `docs(site-002): set D6G1B canonical tip after watchdog cron verification`

Final remote tip after this hash-correction commit is recorded in `D6G1B-DECISION.json`.

Allowlisted paths only under `projects/client-ops-reporting-bridge/`.

MAIN index not mutated. Foreign WIP untouched.

Gate: `D6G1B_CANONICAL_EVIDENCE_COMMIT_PUSHED=YES`
