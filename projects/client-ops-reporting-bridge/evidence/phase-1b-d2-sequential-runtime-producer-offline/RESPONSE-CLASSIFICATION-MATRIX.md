# Response Classification Matrix

202→INTAKE_ACCEPTED terminal; 200 duplicate terminal; 409 conflict terminal; 4xx terminal; 5xx/connect/DNS/TLS future-eligible auto=false; ambiguous timeout manual dedupe check.
