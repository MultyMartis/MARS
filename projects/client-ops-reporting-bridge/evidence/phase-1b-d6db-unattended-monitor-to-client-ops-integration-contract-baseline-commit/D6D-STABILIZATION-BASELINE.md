# D6D-STABILIZATION-BASELINE

No continuous watcher. No daemon. No unbounded sleep.

Bounded scanning uses final artifact names, completion marker (post-deployment), stable size/hash over bounded reads, optional minimum age, deterministic injected clock, completed run receipt/classification.

Historical compatibility remains explicit.
