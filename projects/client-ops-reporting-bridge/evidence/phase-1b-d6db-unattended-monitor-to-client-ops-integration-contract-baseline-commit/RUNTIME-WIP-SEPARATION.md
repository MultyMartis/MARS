# RUNTIME-WIP-SEPARATION — D6DB

`D6DB_RUNTIME_WIP_EXCLUDED_FROM_COMMIT`

Canonical source bytes come only from accepted MAIN working-tree Client Ops paths and surgically constructed marker-only SITE-002 bytes (HEAD + marker).

Runtime path `X:\AI MARS STORAGE\runtime-checkouts\site-002-monitor\repo\...\monitor-02.py` is foreign WIP — not cleaned, not restored, not copied into commit, not deployed.
