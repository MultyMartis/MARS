# Control Tool Surface

**Token:** `D6C2_CONTROL_TOOL_SURFACE_DECLARED`

Hash-locked operator-side surface (see `CONTROL-TOOL-SURFACE.json`).

- Declared/bound for production use: yes
- Source→runtime n8n content deployment: **no**
- Bytes changed during D6C2: lifecycle dry-path extension + new production transport + D6C2 runner + evidence (under `projects/client-ops-reporting-bridge/` only)
- External activation state changes: false→true then true→false only
