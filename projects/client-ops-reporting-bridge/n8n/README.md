# Client Ops n8n locus

**Status:** PHASE 1B-B…C1 + D0 + D1 + D2 + D3 COMPLETE (workflow inactive; durable sequential dedupe retained; D3 controlled live proof; D3 charter consumed)
**Live n8n:** one workflow `MARS Client Ops Bridge — bzpm.ru`; native Header Auth; Pattern B Telegram (`2bIC5376l7ElXb4B`); durable dedupe Data Table `H6VYhwz7RXZCBMmu`; `active=false`; nodes=17; executions=31; rows=2; versionId `3d2fd6fc-bc17-4e0f-b9e5-086c959afd29`; concurrency `DEDUPE_SEQUENTIAL_SAFE_CONCURRENCY_UNPROVEN`
**Architecture:** durable dedupe before runtime producer (D0); R1 preferred; D2 offline producer PROVEN_OFFLINE; D3 controlled synthetic HTTPS PROVEN_LIVE (generic live BLOCKED; charter CONSUMED); post-Telegram SENT ledger deferred; concurrent producers / scheduler / production activation forbidden

| Path | Role |
|------|------|
| `templates/` | Baseline inactive sandbox template (committed; historical blocked-auth create source) |
| `harness/` | Offline Node validator + cases + template gates |
| `runbooks/` | Pre-create / create design / apply-rollback |
| `experience-pack/` | Create + auth + POST + Telegram + D0/D1 architecture facts |
| `runners/` | Create / credential / auth-binding / activation / POST / Telegram / C1 / D0 / D1 runners + validators |
| `evidence/` | Phase 1B-B…C1 + D1 evidence packs |
| `../evidence/phase-1b-d0-runtime-connection-charter/` | D0 decision pack |

See programmer capability extension, Phase 1B-B…D1 docs, and D0/D1 phase charters.
