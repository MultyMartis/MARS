# Runbook — Recover after server component failure

## Steps

1. See recovery guide; restore wrapper/dispatcher from known good; validate.
2. Record evidence paths (no secrets).
3. Stop if production mutation not chartered.
