# Runbook — Temporarily disable outbound dispatch

## Steps

1. Set CLIENT_OPS_DISPATCH_ENABLED false in non-Git local config; verify admin status; imports still run.
2. Record evidence paths (no secrets).
3. Stop if production mutation not chartered.
