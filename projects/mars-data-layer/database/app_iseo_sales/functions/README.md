# app_iseo_sales functions

SQL function definitions for V1 live in migration:

`database/app_iseo_sales/migrations/0003_functions.sql`

Do not duplicate function bodies here. This folder is reserved for future extracted/source-of-truth function files if the migration toolchain splits DDL from `CREATE FUNCTION` scripts.
