# EAR Runtime — requirements manifest (R1.1 skeleton)
#
# Packages intentionally deferred.
# No packages are declared at skeleton phase.
#
# Planned (not installed, not implemented):
#   paramiko — SFTP connector transport (R1; future phase only)
#
# No installation instructions at this phase.
