# config/ — example configuration only

Phase 0 ships **example** PHP config files:

- `app.example.php`
- `database.example.php`

Do not put real credentials here. A future Phase 1 charter may add a config loader that reads local `.env` (never committed).
