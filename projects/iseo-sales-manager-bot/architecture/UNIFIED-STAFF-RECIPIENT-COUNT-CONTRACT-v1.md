# UNIFIED STAFF RECIPIENT COUNT CONTRACT v1

Authoritative count derives from ACCESS_CONTROL active staff eligibility.

CONFIG `pending_reminder_active_recipients_count` is a cache only. `/reminder_status` must prefer live ACCESS when available (Phase 3H.6).
