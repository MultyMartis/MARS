# DIGEST BUTTON TOKEN PROOF — Phase 3H.10

- Callback namespace: `sm:q:<opaque_token>`
- Overflow / all: `sm:q:__all__` → pending list
- Full card from compact view: `sm:f:<token>`
- Cap: **25** lead buttons
- Tokens: opaque FNV/hash of stable `lead_id` — no PII in callback_data
- Unique tokens in harness: PASS
