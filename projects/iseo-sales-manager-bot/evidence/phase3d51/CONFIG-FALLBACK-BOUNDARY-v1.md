# CONFIG FALLBACK BOUNDARY v1

| Allowlist | Status after 3D.5.1 |
|---|---|
| admin_user_ids | Retained — emergency/bootstrap Admin recovery only |
| manager_action_user_ids | Legacy retained in CONFIG sheet — **not** active auth |

`/config` always reports: `Источник прав: реестр ACCESS_CONTROL` with counts from ACCESS_CONTROL.
