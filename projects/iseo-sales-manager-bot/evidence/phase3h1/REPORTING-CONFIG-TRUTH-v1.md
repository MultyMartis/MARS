# Reporting config truth

CONFIG after Phase 3H.1:

- `reporting_sync_enabled=false`
- `reporting_sync_mode=manual`
- `reporting_sync_state=только вручную`
- `reporting_tests_excluded=true`
- `reporting_archive_excluded=true`
- `parser_version=sm-parser-v3.3` (aligned)

Admin Config Summary patched to display:

`Синхронизация отчётности: только вручную`
+ `Тестовые заявки: исключены`
+ `Архивные записи: исключены`
