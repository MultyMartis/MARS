# Test cleanup

- TEST_LEADS data rows cleared (A2:Z1000) → **0**
- REMINDER_DELIVERIES rewritten to contract headers; data rows **0**
- RECIPIENT_REPLIES rewritten to contract headers; data rows **0**
- Production LEADS CLIENT_A untouched
- reporting workbook CLIENT_A untouched
- No historical production deletes
