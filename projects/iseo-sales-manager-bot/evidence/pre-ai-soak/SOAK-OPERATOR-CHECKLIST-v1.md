# Soak operator checklist

Use existing commands: /status /health /stats /pending_count /delivery_status /delivery_users /last_error /reminder_status /reply_profiles /config

At each checkpoint record: workflow actives, errors, new genuine leads, duplicates, Telegram success/fail, recipients, names, pending, stats, reporting, reminders, unexpected test rows.

Do not manufacture production leads.
