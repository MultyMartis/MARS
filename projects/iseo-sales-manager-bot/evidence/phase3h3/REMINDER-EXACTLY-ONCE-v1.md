# Reminder exactly-once

Layers:

1. Window key `pending-reminder:<date>:<time>:<timezone>`
2. Per-recipient REMINDER_DELIVERIES claim/delivered rows
3. CONFIG last-window stamps

Headers repaired; empty ledger at activation baseline.
