# HEALTHCHECK REAL TRIGGER v1

## Real Telegram Trigger /health

**NOT EXECUTED** — no Trigger executions in the activation window.

## Harness /health shape (non-substitute)

```
Проверка Sales Manager

CONFIG: доступна
RAW v2: доступна
CLEAN v2: доступна
LEAD_EVENTS: доступна
ERRORS: доступна
DEDUP_INDEX: доступна
Gmail: привязка найдена, письма не читались
Telegram sandbox: доступен
Operational.dev: выключен, как и требуется
Admin.dev: тестовый режим
ИИ: выключен
Проверка AI: не запускалась
```

Actual-safe reads labelled `доступна`; Gmail not fetched; AI probe skipped.
