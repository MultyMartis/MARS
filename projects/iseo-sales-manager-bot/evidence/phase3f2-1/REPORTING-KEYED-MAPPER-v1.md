# REPORTING KEYED MAPPER v1

Ordered schema contract maps each reporting column → backend field + empty fallback + privacy rule.

Forbidden: `Object.values` row ordering; using header `indexOf` / row numbers as cell values.

Additive column: `Черновик ответа готов` = `Да`/`Нет`. `Готовый первый ответ` = reply text only.
