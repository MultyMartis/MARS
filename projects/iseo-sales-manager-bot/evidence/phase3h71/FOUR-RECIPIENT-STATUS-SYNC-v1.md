# FOUR-RECIPIENT STATUS SYNC — Phase 3H.7.1

`Expand Card Sync Copies` spreads Handle Callback json (`...h`) to all delivered LEAD_DELIVERIES rows for the lead.
Post-patch fields `telegram_callback_reopen` + `edit_keyboard_mode` therefore propagate to all four recipient edits when delivery refs exist.
LIVE_SPAM post-reauth delivery_count observed: 4 each.
