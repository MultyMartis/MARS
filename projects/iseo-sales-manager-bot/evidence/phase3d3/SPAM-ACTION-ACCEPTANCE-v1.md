# SPAM ACTION ACCEPTANCE v1

Conflict path verified after processed: spam rejected with status-already-changed message.  
Applied spam path uses same state machine pending→spam (unit + harness).  
No Gmail delete, no Sheets row delete, no client contact.
