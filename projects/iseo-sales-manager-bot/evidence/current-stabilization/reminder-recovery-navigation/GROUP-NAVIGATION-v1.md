# GROUP NAVIGATION

## Flow

main reminder → `sm:g:<filter>` → compact group message → `sm:q:<lead_token>` → compact lead view.

## Group message

Title + `N лидов · сначала самые старые` + lead buttons (`Имя · возраст`).

## Currentness

Group rebuilds from **current** CLEAN pending at click time. Stale button counts on the morning message are acceptable snapshots.
