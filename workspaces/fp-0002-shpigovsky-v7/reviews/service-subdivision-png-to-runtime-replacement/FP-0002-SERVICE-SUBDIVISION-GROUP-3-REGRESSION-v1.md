# FP-0002 Service Subdivision — GROUP 3 Regression v1

| Target | Result |
|--------|--------|
| GROUP 1 (`f3917bf6` checks via `compiled-validation-group1.py`) | **0 regression** |
| GROUP 2 (`41777d4a` checks via `compiled-validation-group2.py`) | **0 regression** |
| Home (`index.html` — no source edits) | **0 regression** |
| Services V1 | **0 regression** (no source edits) |
| Services V2 (`uslugi-v2.html` — no source edits) | **0 regression** |

GROUP 3 changes scoped to `service-subdivision-stages-v1.html` and scoped SCSS selector extensions only.
