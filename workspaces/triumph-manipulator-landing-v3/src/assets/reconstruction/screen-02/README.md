# Screen 02 Reconstruction Assets

These files are temporary reconstruction support only.

`screen-02-source.png` is a byte-for-byte workspace copy of:

- `projects/triumph-manipulator-landing/design/v1/02.png`

Purpose:

- preserve the three visible manipulator card photos from the V1 Screen 02 authority;
- avoid replacing central equipment imagery with fake CSS art, generic stock, or V2 assets;
- support CSS background crops inside `equipment-pricing-screen-02`.

Production status:

- reconstruction-only;
- not a final standalone equipment photo set;
- should be replaced only after approved individual 5/7/10 tonne manipulator assets are provided.

Source dimensions recorded by `mockups-index.md`:

- `1672 x 941`

Transformation:

- no resize;
- no recompression;
- no crop written to disk;
- CSS uses viewport crops from this source image.
