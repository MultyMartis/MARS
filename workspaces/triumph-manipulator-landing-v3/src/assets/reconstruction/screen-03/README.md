# Screen 03 Reconstruction Assets

These files are temporary reconstruction support only.

`screen-03-source.png` is a byte-for-byte workspace copy of:

- `projects/triumph-manipulator-landing/design/v1/03.png`

Purpose:

- preserve the visible reviewer avatar portraits from the V1 Screen 03 authority;
- avoid replacing visible review evidence with generic stock, CSS portraits, or V2 assets;
- support small CSS background crops inside `trust-reviews-screen-03`.

Production status:

- reconstruction-only;
- not a final standalone review/avatar asset set;
- should be replaced only after approved individual reviewer/media assets or a confirmed live review integration are provided.

Source dimensions recorded by `mockups-index.md`:

- `1672 x 941`

Transformation:

- no resize;
- no recompression;
- no crop written to disk;
- CSS uses viewport crops from this source image.
