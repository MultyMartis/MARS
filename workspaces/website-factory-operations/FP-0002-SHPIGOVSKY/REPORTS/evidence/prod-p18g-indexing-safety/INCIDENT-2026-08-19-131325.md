# INCIDENT — 2026-08-19 13:13:25 indexing closure

**Site:** https://shpigovsky.ru/  
**Wave context:** PROD-P18D-FU01 SMTP closeout  
**Classification:** **A — IndexingControl system mutation via technical closeout script**

---

## Timeline (production Activity Log, UTC+7 local timestamps)

| Time | ID | Action | Actor | Notes |
|------|-----|--------|-------|-------|
| 2026-08-19 11:16:31 | 130 | `indexing_opened` | admin (user_id=2) | Human opened indexing before FU01 |
| 2026-08-19 13:13:19 | 133 | `smtp_test_ok` | System (user_id=0) | FU01 SMTP verify |
| 2026-08-19 13:13:20 | 134 | `smtp_activated` | System (user_id=0) | FU01 activate delivery |
| **2026-08-19 13:13:25** | **135** | **`indexing_closed`** | **System (user_id=0)** | **Incident event** |
| 2026-08-19 19:18:26 | 139 | `indexing_opened` | admin (user_id=2) | Human reopen (Olya/operator) |

---

## Root cause (proven)

1. **P18D-FU01** fresh intake found live `blog_public=1` (indexing already human-open at 11:16).
2. Closeout script `_fu01_runtime_closeout.py` function `close_indexing()` executed:
   ```php
   \Shpigovsky\Core\Admin\IndexingControl::set_site_indexability(false);
   ```
   via remote `wp_eval` with **no logged-in WordPress user** → Activity Log `user_id=0` → UI label **«Система»**.
3. This was **not** WP-CLI, WPilot, robots file mutation alone, or Dashboard UI — it was **authorized FU01 closeout logic** treating historical baseline «CLOSED» as authority over live human-open state.
4. Evidence: `REPORTS/evidence/prod-p18d-fu01-smtp-closeout/10-indexing-close.json`, `_fu01_runtime_closeout.py` lines 594–610.

---

## Robots at incident time (historical)

FU01 report states FU01 wrote IndexingControl canonical closed robots (`Disallow: /`).  
Current production (post-Olya reopen, pre-P18G) uses a **host-managed physical `robots.txt`** without global `Disallow: /` — not IndexingControl’s simple template.  
**Owner today:** physical file in docroot; IndexingControl sync applies only when file matches known closed/open templates or global disallow must be cleared.

---

## Attribution gap

«Система» = **missing actor context** on programmatic `log_system_event()` / `set_site_indexability()` calls (user_id 0).  
P18G adds `source` suffix in Activity Log titles and `IndexingControl::request_state()` context.

---

## Remediation (P18G)

- Non-human OPEN→CLOSED blocked by default (`request_state` + `pre_update_option_blog_public` guard).
- Human authority metadata (`fp02_indexing_human_authority`).
- Multi-surface effective state (`IndexingState`).
- Administrator critical alerts + hourly watchdog (alert-only).
- Documentation: INDEXING OPEN — HUMAN-APPROVED is current truth; FU01 closeout must not repeat.

---

*Evidence: `REPORTS/evidence/prod-p18g-indexing-safety/01-pre-intake.json` activity_window.*
