# Libertinus Serif — asset status

Status: WAITING_FOR_OPERATOR_ASSET

Searched: theme `assets/fonts`, INCOMING/OPERATOR-ASSETS, FP-0002 tree, Storage *Libertinus* — no files.

Existing local Inter pattern (`v9-style.css` @font-face + `assets/fonts/inter/*.woff2`, `font-display`).

Required for current computed weight 400 (heading):

- `LibertinusSerif-Regular.woff2` (preferred) or equivalent Regular WOFF2
- Optional: none unless operator requests bold (current weight is 400)

Do not CDN / do not substitute.
