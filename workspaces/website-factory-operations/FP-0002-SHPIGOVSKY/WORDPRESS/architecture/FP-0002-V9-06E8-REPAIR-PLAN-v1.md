# FP-0002 V9-06E8 Repair Plan v1

## Strategy

Template-managed V9 static fallbacks in `inc/v9-static-content.php` — **no invented copy**, **no DB writes** unless ACF overlay required (not needed for E8 scope).

## Components

1. **Services hub** — override CPT-derived mutated text with `uslugi-v2.html` authority; fix `program-cta-band` container wrap.
2. **Contacts** — restore full V9 location card structure + theme map PNGs + rehabilitation photo.
3. **Alcohol leaf** — restore full `usluga-konechnaya-v1` section stack in `alcohol-stack.php`.
4. **Mental health / eating disorders** — keep subdivision template; classify DEMO (placeholder static V9 pages).

Evidence: `validation/v9-06e8-static-v9-content-main-layout-authority-repair/repair-plan.json`
