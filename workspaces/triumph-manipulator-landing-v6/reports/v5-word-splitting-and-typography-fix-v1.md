# V5 — word-splitting CSS removal + RU typography audit (v1)

**Workspace:** `workspaces/triumph-manipulator-landing-v5/`  
**Date:** 2026-05-24  
**Scope:** CSS word-breaking rules in `src/scss/` + visible HTML typography (`&nbsp;`)  
**Build:** `npm run build` — **PASS** (exit 0, ~1.26s)

---

## 1. Dangerous CSS properties — scan results

| Property | Value | Found in `src/scss`? |
|----------|-------|----------------------|
| `overflow-wrap` | `anywhere` | **No** |
| `word-break` | `break-word` | **Yes** — 1 occurrence (removed) |
| `word-break` | `break-all` | **No** |
| `word-wrap` | `break-word` | **No** |
| `hyphens` | `auto` | **No** |

**Additional context:** many UI selectors used `overflow-wrap: break-word` (allowed globally on `body`, but risky on headings/buttons/nav/cards/FAQ/forms). Those were normalized per task rules.

---

## 2. CSS replacements

### Safe rules applied (`overflow-wrap: normal; word-break: normal; hyphens: manual`)

| File | Selectors / context |
|------|---------------------|
| `src/scss/utils/_section-headings.scss` | `@mixin section-h2-overflow-safe` (all section H2 headings via mixin) |
| `src/scss/components/_button.scss` | `.button` @ `max-width: 760px` |
| `src/scss/components/_forms.scss` | `.site-form__consent-text`, `.site-form__consent-text a` — also removed `word-break: break-word` on links |
| `src/scss/layout/_header.scss` | `.site-header__nav-link`, `.site-header__phone`, `.site-header__cta` |
| `src/scss/sections/_dark-proof-strip.scss` | `.proof-strip__item strong`, `.proof-strip__item span` (mobile) |
| `src/scss/sections/_landing-footer.scss` | footer nav/contact/legal links, requisites (mobile) |
| `src/scss/sections/_screen-02-prices.scss` | `.machine-showcase__title`, `.machine-showcase__specs dd`, `.machine-transport__heading` (+ spans), cards/lists (mobile) |
| `src/scss/sections/_screen-04-faq.scss` | `.faq-item`, `.faq-item summary` |
| `src/scss/sections/_v5-hero-extensions.scss` | `.hero--v5 .hero__title`, `.hero__title span`, `.hero-form__title` |
| `src/scss/sections/_v5-page01-overrides.scss` | PPC mobile overrides: headings, cards, proof strip, `.button`, transport/showcase titles |

### Intentionally kept `overflow-wrap: break-word`

| File | Selector | Reason |
|------|----------|--------|
| `src/scss/base/_base.scss` | `body` | long body copy default |
| `src/scss/sections/_screen-04-faq.scss` | `.faq-item__body p` | long FAQ answer paragraphs |

**Not changed:** `font-size`, layout grids, section structure, `meta`, `alt`, JSON-LD, `data-*`, `href`, `src`.

---

## 3. HTML typography (`&nbsp;`) — visible text only

Fixed **obvious** phrase-break risks (short prepositions, number+unit, card labels). **18 partials** updated:

- `src/partials/layout/header-v5-page01.html` — «Без&nbsp;посредников» (drawer note)
- `src/partials/sections/screen-01-hero.html` — «Подача&nbsp;от»
- `src/partials/sections/v5-page01/screen-01-hero.html` — hero title/lead/notice/proof labels (aligned with PPC 5-tonn pattern)
- `src/partials/sections/v5-page01/dark-proof-strip.html` — «Без&nbsp;посредников»
- `src/partials/sections/v5-page01/landing-footer.html` — brand line «Манипулятор&nbsp;5&nbsp;тонн…»
- `src/partials/sections/legal/landing-footer-legal.html` — same brand line
- **12×** `src/partials/sections/v5-ppc/*/screen-01-hero.html` — «От&nbsp;30&nbsp;минут», «Для&nbsp;юрлиц&nbsp;—&nbsp;безнал»; v5-page01 baseline hero strings where applicable

**Examples of fixes:**
- `Манипулятор 5&nbsp;тонн` → `Манипулятор&nbsp;5&nbsp;тонн`
- `От 30&nbsp;минут` → `От&nbsp;30&nbsp;минут`
- `Без посредников` → `Без&nbsp;посредников`
- Hero lead / notice: non-breaking ties for «и», «по», «в», commas in cargo lists (v5-page01 baseline)

Most PPC landings were already typographed; this pass closed gaps on v5-page01 baseline and shared hero-proof labels.

---

## 4. Not touched

- `meta` tags, `alt` attributes, JSON-LD blocks
- `href`, `src`, `data-*` attributes
- `font-size` (no shrink-to-fit workaround)
- Layout / grid / section markup structure
- Meaning of copy (no rewrites)
- `dist/` (regenerated by build)
- Git commit / push (per task)

---

## 5. Build status

```
npm run build  →  gulp build  →  exit 0
Tasks: cleanDist, html, styles, scripts, images, favicon, vendorFontawesome, fonts — all finished
```

Sass deprecation notice (`legacy-js-api`) — pre-existing, non-blocking.

---

## 6. SAFE UNKNOWN

| Item | Status |
|------|--------|
| Visual QA on all breakpoints after CSS change | **UNKNOWN** — build passes; no browser screenshot pass in this task |
| Overflow on ultra-narrow viewports (<360px) for long untranslated English tokens | **UNKNOWN** — Russian copy typographed; rare Latin tokens may still wrap |
| Whether `text-wrap: pretty` / `balance` interact with `overflow-wrap: normal` on all target browsers | **UNKNOWN** — properties retained where already present |
| Full exhaustive RU typography (em dash, curly quotes) on every partial | **NOT claimed** — only explicit line-break risks per task scope |
| Workspace git tracking | **UNKNOWN** — `workspaces/` may be gitignored in MARS root; changes live locally in workspace |

---

## 7. Changed files summary

**SCSS (10):**  
`_section-headings.scss`, `_button.scss`, `_forms.scss`, `_header.scss`, `_dark-proof-strip.scss`, `_landing-footer.scss`, `_screen-02-prices.scss`, `_screen-04-faq.scss`, `_v5-hero-extensions.scss`, `_v5-page01-overrides.scss` (+ compiled CSS via build)

**HTML (18):** see §3

**Report (1):** this file
