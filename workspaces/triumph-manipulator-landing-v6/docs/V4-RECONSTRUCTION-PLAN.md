# Triumph Manipulator Landing V4 — Reconstruction Plan

## 1. Run Model

V4 is a clean reconstruction run from fresh initialization.

V4 is not a continuation of V3. V3 may be used only as:

- Governance lesson source.
- Drift-history source.
- Anti-pattern evidence.
- Reconstruction failure history.

V3 implementation files are not authority.

## 2. Current Task Boundary

This task performs only:

- Initialization.
- Audit.
- Decomposition.
- Source analysis.
- Reconstruction planning.
- Authority mapping.
- Shell/system preparation.

This task does not perform:

- Full hero rebuild.
- Screen 02 rebuild.
- Section implementation marathon.
- Production-readiness claim.
- Pixel-perfect claim.
- Autonomous reconstruction claim.

## 3. Workspace Structure

Created workspace:

`workspaces/triumph-manipulator-landing-v4/`

Planned clean structure:

```text
src/
dist/
docs/
src/pages/
src/partials/layout/
src/partials/sections/
src/partials/components/
src/scss/
src/js/
src/assets/
```

No V3 implementation files are copied into this workspace.

## 4. Phase Plan

### Phase A — Source Confirmation

1. Confirm that `design/v1/01.png` through `04.png` are visible on disk.
2. Open and inspect each V1 raster.
3. Record screen-level findings without naming implementation partials prematurely.
4. Confirm whether any non-raster source accompanies V1.

Stop condition:

- If V1 raster files remain unavailable, mark reconstruction source boundary SAFE UNKNOWN and request human input.

### Phase B — First-Screen Decomposition

1. Separate header system, first-screen shell, hero content system, and background ownership.
2. Confirm whether any slider behavior is source-proven or only visually suggested.
3. Map mobile header survivability requirements.
4. Lock Russian typography handling for any extracted HTML copy.

### Phase C — Section Authority Matrix

For each V1 strip:

- Source file.
- Observed semantic role.
- Copy locks.
- Image/background authority.
- CTA/proof rhythm.
- Atmosphere transition.
- Responsive risk.
- SAFE UNKNOWN.

### Phase D — Asset Placement Map

1. Match `shared-assets/` files to source-proven placements only.
2. Use approved files as-is.
3. Record all missing assets as SAFE UNKNOWN.
4. Do not create derived assets without documented reason.

### Phase E — Shell Preparation

After source confirmation only:

- Create minimal page shell.
- Create partial boundaries based on source analysis.
- Create SCSS entry points only when implementation is authorized.
- Keep header/hero/slider separation visible in file structure.

## 5. Implementation Discipline for Later Phases

Future implementation must:

- Stay source-bound to V1.
- Avoid V3 code reuse.
- Avoid V3 responsive patches.
- Avoid V3 local overrides.
- Keep docs updated when source assumptions change.
- Preserve Russian non-breaking typography in HTML.
- Keep generated/derived asset lineage documented.

## 6. Review Cadence

Recommended human-supervised review points:

1. After V1 raster visibility is confirmed.
2. After first-screen decomposition.
3. After section authority matrix.
4. Before first HTML/SCSS implementation.
5. After each screen/section reconstruction slice.

## 7. Validation Plan

Use PowerShell-compatible commands only.

Required checks for this planning stage:

- Reference search for V4 docs and forbidden V3 contamination language.
- Markdown diagnostics where available.
- Shell-safe command syntax.
- `git diff --check`.

Future implementation validation should add:

- Build command, once package/build stack exists.
- HTML validation, if tooling exists.
- Responsive evidence, once rendered UI exists.
- Accessibility review proportional to implemented surface.

## 8. SAFE UNKNOWN

- Concrete V1 raster files were not confirmed by file search in this task.
- Exact V4 build stack is not initialized.
- Exact section decomposition requires direct V1 visual inspection.
- No implementation readiness is claimed.
